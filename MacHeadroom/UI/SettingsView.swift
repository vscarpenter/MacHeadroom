import SwiftUI

struct SettingsView: View {
  var body: some View {
    Text("\(AppIdentity.displayName) settings land in the next phase.")
      .padding()
      .frame(width: 360, height: 160)
  }
}
